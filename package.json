{
  "name": "gm-paranoia",
  "version": "1.0.0",
  "description": "",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rm -r dist; tsc && copyfiles -u 1 src/**/*.json src/**/*.css src/**/*.hbs dist/ && copyfiles LICENSE README.md dist/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/taragnor/gm-paranoia-taragnor"
  },
  "keywords": [
    "FoundryVTT",
    "Security"
  ],
  "author": "taragnor",
  "license": "ISC",
  "devDependencies": {
    "@league-of-foundry-developers/foundry-vtt-types": ">=9.280.0",
    "copyfiles": "^2.4.1",
    "typescript": ">=4.8.3"
  }
}
